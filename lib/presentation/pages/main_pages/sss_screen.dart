import 'package:flutter/material.dart';

class SSSScreen extends StatelessWidget {
  const SSSScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SSS', style: TextStyle(fontSize: 24)),
    );
  }
}
